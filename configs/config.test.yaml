server:
  port: 18080

mysql:
  host: 127.0.0.1
  port: 3306
  user: eino_risk_qa
  password: "EinoRiskQa#2026"
  database: eino_risk_qa_test
  charset: utf8mb4
  parse_time: true
  loc: Local

llm:
  provider: mock
  vision_provider: ""
  max_rounds: 3
  attack_detector:
    enabled: true
    confidence_threshold: 0.85
    timeout_seconds: 10

storage:
  local_dir: "./data/test-uploads"
  max_file_bytes: 1048576
  max_stored_image_bytes: 1048576
  max_files_per_question: 3
  allowed_mime_types:
    - "image/jpeg"
    - "image/png"
    - "image/webp"

auth:
  api_key: "test-api-key"
